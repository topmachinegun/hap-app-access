# 参数命名规范

<cite>
**本文档引用的文件**
- [README.md](file://README.md)
- [SKILL.md](file://SKILL.md)
</cite>

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构概览](#架构概览)
5. [详细组件分析](#详细组件分析)
6. [依赖关系分析](#依赖关系分析)
7. [性能考虑](#性能考虑)
8. [故障排除指南](#故障排除指南)
9. [结论](#结论)

## 简介

本文档详细阐述明道云 HAP 应用的参数命名规范，重点关注驼峰命名法的使用规则。该规范适用于两种调用路径：MCP 协议和 V3 REST API，确保在不同技术栈中的参数一致性。

明道云 HAP 应用提供两种授权类型与两种调用路径的交叉组合，为开发者和 AI 工具提供灵活的访问方式。本文档的核心目标是建立统一的参数命名标准，避免常见的命名错误，提高代码的可维护性和一致性。

## 项目结构

该项目采用简洁的文档结构，主要包含两个核心文件：

```mermaid
graph TB
Root[项目根目录] --> README[README.md<br/>项目概述]
Root --> SKILL[SKILL.md<br/>详细技能文档]
README --> Content1[项目介绍]
README --> Content2[安装说明]
README --> Content3[技能结构]
SKILL --> Content4[授权类型]
SKILL --> Content5[调用路径]
SKILL --> Content6[交叉矩阵]
SKILL --> Content7[通用规范]
SKILL --> Content8[陷阱清单]
```

**图表来源**
- [README.md:1-53](file://README.md#L1-L53)
- [SKILL.md:1-436](file://SKILL.md#L1-L436)

**章节来源**
- [README.md:1-53](file://README.md#L1-L53)
- [SKILL.md:1-436](file://SKILL.md#L1-L436)

## 核心组件

### 驼峰命名规范

明道云 HAP 应用严格遵循驼峰命名法（camelCase），这是参数命名的核心原则：

#### 标准驼峰命名参数

| 参数名称 | 类型 | 用途 | 示例值 |
|---------|------|------|--------|
| pageSize | number | 每页记录数 | 50, 100, 200 |
| pageIndex | number | 页码索引 | 1, 2, 3 |
| useFieldIdAsKey | boolean | 使用字段ID作为键 | true, false |
| worksheetId | string | 工作表ID | "xxxx-xxxx-xxxx" |

#### 错误的命名方式

以下命名方式是不被接受的：

- `page_size` → 应使用 `pageSize`
- `page_index` → 应使用 `pageIndex`  
- `use_field_id_as_key` → 应使用 `useFieldIdAsKey`
- `worksheet_id` → 应使用 `worksheetId`

**章节来源**
- [SKILL.md:250-255](file://SKILL.md#L250-L255)

### 调用路径兼容性

驼峰命名规范在两种调用路径中保持一致：

#### MCP 协议中的参数使用

在 MCP 协议中，工具调用参数同样遵循驼峰命名：

```mermaid
sequenceDiagram
participant Client as 客户端
participant MCP as MCP协议
participant HAP as 明道云HAP
Client->>MCP : 工具调用请求
MCP->>HAP : 发送带驼峰参数的请求
HAP->>HAP : 验证参数命名
HAP-->>MCP : 返回结果
MCP-->>Client : 响应数据
```

#### V3 REST API 中的参数使用

在 V3 REST API 中，请求体参数同样使用驼峰命名：

```mermaid
sequenceDiagram
participant Client as 客户端
participant API as V3 REST API
participant HAP as 明道云HAP
Client->>API : POST /v3/app/worksheets/{id}/rows/list
API->>HAP : 发送带驼峰参数的请求
HAP->>HAP : 验证参数命名
HAP-->>API : 返回结果
API-->>Client : JSON响应
```

**章节来源**
- [SKILL.md:127-162](file://SKILL.md#L127-L162)

## 架构概览

### 参数命名一致性架构

```mermaid
graph TB
subgraph "参数命名规范"
A[驼峰命名法] --> B[统一标准]
B --> C[MCP协议]
B --> D[V3 REST API]
end
subgraph "标准参数集合"
E[pageSize]
F[pageIndex]
G[useFieldIdAsKey]
H[worksheetId]
end
subgraph "错误模式"
I[下划线命名]
J[混合命名]
K[首字母大写]
end
C --> E
C --> F
C --> G
C --> H
D --> E
D --> F
D --> G
D --> H
I --> L[参数验证失败]
J --> L
K --> L
```

**图表来源**
- [SKILL.md:250-255](file://SKILL.md#L250-L255)
- [SKILL.md:127-162](file://SKILL.md#L127-L162)

### 参数命名验证流程

```mermaid
flowchart TD
Start([开始参数命名]) --> CheckFormat["检查命名格式"]
CheckFormat --> IsCamel{"是否为驼峰命名？"}
IsCamel --> |是| ValidateType["验证参数类型"]
IsCamel --> |否| Reject["拒绝请求"]
ValidateType --> TypeCheck{"参数类型正确？"}
TypeCheck --> |是| ValidateValue["验证参数值范围"]
TypeCheck --> |否| Reject
ValidateValue --> ValueCheck{"值在有效范围内？"}
ValueCheck --> |是| Accept["接受请求"]
ValueCheck --> |否| Reject
Reject --> Error["返回参数错误"]
Accept --> Success["处理成功"]
```

## 详细组件分析

### 标准参数详解

#### pageSize 参数

**用途**：控制每次请求返回的记录数量

**使用场景**：
- 大数据集分页查询
- 性能优化和内存管理
- 响应时间控制

**最佳实践**：
- MCP 协议：建议使用 50（单次响应约 256KB 上限）
- V3 REST API：建议使用 100-500（无缓冲限制）

**错误示例**：
- ❌ `page_size: 100` → 应使用 `pageSize: 100`
- ❌ `page-size: 50` → 应使用 `pageSize: 50`

#### pageIndex 参数

**用途**：指定要获取的页码

**使用场景**：
- 分页数据遍历
- 无限滚动加载
- 数据导出和备份

**注意事项**：
- 从 1 开始计数（非 0）
- 需要与 pageSize 配合使用
- 越界时返回空数据

**错误示例**：
- ❌ `page_index: 0` → 应使用 `pageIndex: 1`
- ❌ `PageIndex: 1` → 应使用 `pageIndex: 1`

#### useFieldIdAsKey 参数

**用途**：控制返回数据的键使用字段ID还是别名

**使用场景**：
- 字段ID与别名混用
- 数据迁移和同步
- API 兼容性处理

**重要说明**：
- 当启用时，返回数据的键强制使用字段ID（UUID）
- 这是避免数据错配的关键参数

**错误示例**：
- ❌ `use_field_id_as_key: true` → 应使用 `useFieldIdAsKey: true`
- ❌ `UseFieldIdAsKey: false` → 应使用 `useFieldIdAsKey: false`

#### worksheetId 参数

**用途**：标识具体的工作表

**使用场景**：
- 多工作表数据操作
- 工作表切换
- 数据隔离

**格式要求**：
- 必须是有效的 UUID 格式
- 区分大小写
- 不能包含特殊字符

**错误示例**：
- ❌ `worksheet_id: "xxx"` → 应使用 `worksheetId: "xxx"`
- ❌ `WorksheetId: "xxx"` → 应使用 `worksheetId: "xxx"`

### 参数命名错误类型

#### 常见错误类型

```mermaid
classDiagram
class 参数命名错误 {
+下划线命名错误
+混合大小写错误
+首字母大写错误
+拼写错误
}
class 下划线命名错误 {
+page_size → pageSize
+use_field_id_as_key → useFieldIdAsKey
+worksheet_id → worksheetId
}
class 混合大小写错误 {
+PageIndex → pageIndex
+PageSize → pageSize
+UseFieldIdAsKey → useFieldIdAsKey
}
class 首字母大写错误 {
+PageIndex → pageIndex
+WorksheetId → worksheetId
+UseFieldIdAsKey → useFieldIdAsKey
}
参数命名错误 --> 下划线命名错误
参数命名错误 --> 混合大小写错误
参数命名错误 --> 首字母大写错误
```

**图表来源**
- [SKILL.md:250-255](file://SKILL.md#L250-L255)

#### 错误影响分析

| 错误类型 | 影响程度 | 常见表现 | 解决方案 |
|---------|---------|---------|---------|
| 下划线命名 | 高 | 参数被忽略，返回默认值 | 统一改为驼峰命名 |
| 混合大小写 | 中 | 部分参数生效，部分不生效 | 严格遵循驼峰命名规则 |
| 首字母大写 | 低 | 个别参数不识别 | 遵循小驼峰命名 |
| 拼写错误 | 高 | 参数验证失败，返回错误 | 使用标准参数名 |

**章节来源**
- [SKILL.md:380-389](file://SKILL.md#L380-L389)

### 调用路径中的参数一致性

#### MCP 协议参数使用

在 MCP 协议中，工具调用参数同样遵循驼峰命名：

```mermaid
sequenceDiagram
participant AI as AI助手
participant MCP as MCP服务器
participant HAP as HAP服务
AI->>MCP : get_record_list(pageSize=50, pageIndex=1)
MCP->>HAP : 转发驼峰参数
HAP->>HAP : 验证参数命名
HAP-->>MCP : 返回记录列表
MCP-->>AI : 响应数据
```

#### V3 REST API 参数使用

在 V3 REST API 中，请求体参数使用驼峰命名：

```mermaid
sequenceDiagram
participant Client as 客户端
participant API as V3 API
participant HAP as HAP服务
Client->>API : POST /rows/list
API->>HAP : 发送 {pageSize : 50, pageIndex : 1}
HAP->>HAP : 验证驼峰参数
HAP-->>API : 返回结果
API-->>Client : JSON响应
```

**章节来源**
- [SKILL.md:127-162](file://SKILL.md#L127-L162)

## 依赖关系分析

### 参数命名依赖关系

```mermaid
graph LR
subgraph "参数依赖关系"
A[pageSize] --> B[pageIndex]
C[useFieldIdAsKey] --> D[数据结构]
E[worksheetId] --> F[工作表访问]
B --> G[分页逻辑]
D --> H[字段映射]
F --> I[权限验证]
G --> J[性能影响]
H --> K[数据准确性]
I --> L[安全控制]
end
```

### 参数验证依赖

```mermaid
flowchart TD
Params[参数输入] --> Validate[参数验证]
Validate --> CamelCheck{驼峰命名检查}
CamelCheck --> |通过| TypeCheck{类型检查}
CamelCheck --> |失败| Error1[返回10参数错误]
TypeCheck --> RangeCheck{范围检查}
RangeCheck --> |通过| Success[处理请求]
RangeCheck --> |失败| Error2[返回参数错误]
Error1 --> Log[记录错误日志]
Error2 --> Log
Success --> Log
```

**图表来源**
- [SKILL.md:380-389](file://SKILL.md#L380-L389)

**章节来源**
- [SKILL.md:280-288](file://SKILL.md#L280-L288)

## 性能考虑

### 参数命名对性能的影响

虽然参数命名本身不影响计算性能，但错误的命名会导致：

1. **请求失败**：参数被忽略，需要重新发送
2. **额外网络开销**：重复请求增加网络负载
3. **处理延迟**：错误处理和重试增加响应时间

### 最佳实践建议

- 在代码中统一使用驼峰命名常量
- 建立参数命名检查机制
- 使用 IDE 插件或 ESLint 规则自动检查
- 在 API 文档中明确参数命名规范

## 故障排除指南

### 常见参数命名错误排查

#### 错误代码 10（参数错误）

当出现参数错误时，检查以下方面：

1. **参数命名**：确认使用驼峰命名而非下划线
2. **参数类型**：确认数值参数使用数字而非字符串
3. **参数范围**：确认 pageSize 在允许范围内
4. **参数完整性**：确认必需参数均已提供

#### 调试步骤

```mermaid
flowchart TD
Start([开始调试]) --> CheckParams["检查参数命名"]
CheckParams --> FixNaming["修正命名格式"]
FixNaming --> VerifyTypes["验证参数类型"]
VerifyTypes --> FixTypes["修正参数类型"]
FixTypes --> TestRequest["测试请求"]
TestRequest --> Success{"请求成功？"}
Success --> |是| Complete["完成调试"]
Success --> |否| CheckError["检查错误代码"]
CheckError --> FixError["根据错误代码修正"]
FixError --> TestRequest
```

#### 错误代码对照表

| 错误代码 | 含义 | 解决方案 |
|---------|------|---------|
| 10 | 参数错误 | 检查参数命名和类型 |
| 10001 | HTTP Headers 验证失败 | 检查 OAuth 域名白名单 |
| 600101 | 授权已失效 | 刷新 Bearer Token |

**章节来源**
- [SKILL.md:378-398](file://SKILL.md#L378-L398)

## 结论

明道云 HAP 应用的参数命名规范以驼峰命名法为核心，确保在 MCP 协议和 V3 REST API 两种调用路径中的一致性。通过严格执行参数命名标准，可以：

1. **提高代码质量**：统一的命名规范减少歧义和错误
2. **增强系统稳定性**：避免因参数命名错误导致的请求失败
3. **提升开发效率**：减少调试和错误排查时间
4. **保证 API 兼容性**：确保不同调用路径的参数一致性

建议开发者在项目初期就建立参数命名规范，并在团队内部推广使用。通过代码审查和自动化检查工具，确保规范得到严格执行。